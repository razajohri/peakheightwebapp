# ===========================================
# PeakHeight Web App Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values

# -------------------------------------------
# SUPABASE
# -------------------------------------------
# Your Supabase project URL and keys
NEXT_PUBLIC_SUPABASE_URL=https://ffdtcjigdccrbxjcizko.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service role key (for server-side operations like webhooks)
# Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# -------------------------------------------
# REVENUECAT
# -------------------------------------------
# RevenueCat API key for web
# Get this from RevenueCat Dashboard > Project Settings > API Keys > Web
NEXT_PUBLIC_REVENUECAT_API_KEY=your_revenuecat_web_api_key

# RevenueCat webhook authorization header
# Set this in RevenueCat Dashboard > Project Settings > Webhooks
# Use a secure random string
REVENUECAT_WEBHOOK_AUTH_HEADER=your_secure_webhook_secret

# -------------------------------------------
# STRIPE (used by RevenueCat for web billing)
# -------------------------------------------
# Stripe publishable key (for client-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_publishable_key

# Stripe secret key (for server-side)
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key

# -------------------------------------------
# APP CONFIGURATION
# -------------------------------------------
# Your production domain
NEXT_PUBLIC_APP_URL=https://peakheight.app

# App Store links (update with your actual app IDs)
NEXT_PUBLIC_IOS_APP_LINK=https://apps.apple.com/us/app/peak-height/id6752793377
NEXT_PUBLIC_ANDROID_APP_LINK=https://play.google.com/store/apps/details?id=com.peakheight.app

# -------------------------------------------
# SETUP INSTRUCTIONS
# -------------------------------------------
# 
# 1. SUPABASE SETUP:
#    a. Go to Supabase Dashboard > Settings > API
#    b. Copy the URL and anon key
#    c. Copy the service_role key (keep secret!)
#    d. Enable Apple and Google auth providers:
#       - Settings > Authentication > Providers
#       - Configure Apple (need Apple Developer account)
#       - Configure Google (need Google Cloud Console)
#
# 2. REVENUECAT SETUP:
#    a. Create a project at https://app.revenuecat.com
#    b. Add Web as a platform
#    c. Connect your Stripe account
#    d. Create products (weekly, yearly)
#    e. Create an offering with those products
#    f. Get the Web API key from Project Settings > API Keys
#    g. Set up webhook:
#       - Go to Project Settings > Webhooks
#       - Add webhook URL: https://yourdomain.com/api/revenuecat/webhook
#       - Set authorization header
#
# 3. STRIPE SETUP (for RevenueCat Web Billing):
#    a. Create account at https://stripe.com
#    b. Create products matching your RevenueCat products
#    c. Get API keys from Developers > API Keys
#    d. Connect Stripe to RevenueCat in RC dashboard
#
# 4. APPLE SIGN-IN FOR WEB:
#    a. Go to Apple Developer > Certificates, IDs & Profiles
#    b. Create a Services ID for web
#    c. Configure domains and return URLs
#    d. Add credentials to Supabase Apple provider
#
# 5. GOOGLE SIGN-IN FOR WEB:
#    a. Go to Google Cloud Console
#    b. Create OAuth 2.0 credentials
#    c. Add authorized redirect URIs
#    d. Add credentials to Supabase Google provider
